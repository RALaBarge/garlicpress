# Project Skeleton
# Root: /home/jinx/ai-stack/garlicpress
# Files: 13

## File Tree
garlicpress/
  ├── __init__.py
  ├── cli.py
  ├── client.py
  ├── config.py
  ├── map_agent.py
  ├── queue.py
  ├── reduce.py
  ├── schema.py
  ├── skeleton.py
  ├── swap.py
  ├── __init__.py
  ├── test_schema.py
  ├── test_skeleton.py

## API Surface

### garlicpress/cli.py
def _get_client(api_key: str | None, base_url: str | None) -> LLMClient: ...
def cli() -> None: ...
def run(repo_path: Path, output: str, concurrency: int, map_model: str, reduce_model: str, swap_model: str, spec_files: tuple[str, ...], skip_swap: bool, changed_only: bool, base_ref: str, base_url: str | None, api_key: str | None, verbose: bool) -> None: ...
async def _run_all(repo_root: Path, findings_root: Path, config: Config, skip_swap: bool, api_key: str | None=None, base_url: str | None=None) -> None: ...
def _print_summary(results, summaries, swap_report, map_s, reduce_s, swap_s): ...
def map_cmd(repo_path, output, concurrency, model, changed_only, base_ref, base_url, api_key, verbose): ...
async def _run_map_only(repo_root, findings_root, config, api_key=None, base_url=None): ...
def reduce_cmd(findings_dir, model, base_url, api_key, verbose): ...
async def _run_reduce_only(findings_root, config, api_key=None, base_url=None): ...
def swap_cmd(findings_dir, spec_files, model, base_url, api_key, verbose): ...
async def _run_swap_only(findings_root, config, api_key=None, base_url=None): ...
async def _bounded(task): ...

### garlicpress/client.py
def _is_ollama(base_url: str | None) -> bool: ...
class LLMClient():
    def __init__(self, api_key: str | None, base_url: str | None) -> None: ...
    async def complete(self, *, model: str, max_tokens: int, system: str, messages: list[dict]) -> str: ...
    async def _complete_ollama(self, model: str, max_tokens: int, system: str, messages: list[dict]) -> str: ...
    async def _complete_openai(self, model: str, max_tokens: int, system: str, messages: list[dict]) -> str: ...
def __init__(self, api_key: str | None, base_url: str | None) -> None: ...
async def complete(self, *, model: str, max_tokens: int, system: str, messages: list[dict]) -> str: ...
async def _complete_ollama(self, model: str, max_tokens: int, system: str, messages: list[dict]) -> str: ...
async def _complete_openai(self, model: str, max_tokens: int, system: str, messages: list[dict]) -> str: ...

### garlicpress/config.py
class Config():
    def prompt(self, name: str) -> str: ...
def prompt(self, name: str) -> str: ...

### garlicpress/map_agent.py
def _clean_findings(data: dict) -> dict: ...
def _repair_json_escapes(text: str) -> str: ...
def _extract_json_object(text: str) -> str: ...
def _normalize_data(data: dict, task: 'MapTask') -> dict: ...
def _make_stub(task: 'MapTask', raw: str) -> 'FileFindingsReport': ...
async def run_map_agent(task: MapTask, client: LLMClient, config: Config, skeleton_text: str) -> FileFindingsReport | None: ...
def _write_findings(task: MapTask, report: FileFindingsReport) -> None: ...

### garlicpress/queue.py
def _git_last_modified(repo_root: Path, file_path: Path) -> str | None: ...
def _git_files_changed_since(repo_root: Path, since_date: str) -> set[str]: ...
def _git_is_repo(path: Path) -> bool: ...
class MapTask():
    def file_id(self) -> str: ...
def build_queue(repo_root: Path, findings_root: Path, changed_only: bool=False, base_ref: str='HEAD~1') -> list[MapTask]: ...
def file_id(self) -> str: ...

### garlicpress/reduce.py
def _load_file_findings(dir_path: Path) -> list[FileFindingsReport]: ...
def _load_child_summaries(dir_path: Path) -> list[DirectorySummary]: ...
def _build_reduce_input(dir_path: Path, findings_root: Path, file_reports: list[FileFindingsReport], child_summaries: list[DirectorySummary]) -> str: ...
async def reduce_directory(dir_path: Path, findings_root: Path, client: LLMClient, config: Config) -> DirectorySummary | None: ...
async def reduce_tree(findings_root: Path, client: LLMClient, config: Config) -> list[DirectorySummary]: ...

### garlicpress/schema.py
class Severity(str, Enum):
class FindingType(str, Enum):
class Traceability(BaseModel):
class Finding(BaseModel):
class ImplicitAssumption(BaseModel):
class DependencyChange(BaseModel):
class FileFindingsReport(BaseModel):
class Contradiction(BaseModel):
class DirectorySummary(BaseModel):
class SwapFinding(BaseModel):
class SwapReport(BaseModel):
class FinalReport(BaseModel):
def findings_json_schema() -> str: ...
def reduce_json_schema() -> str: ...
def swap_json_schema() -> str: ...

### garlicpress/skeleton.py
def _build_tree_lines(root: Path, paths: list[Path]) -> list[str]: ...
def _python_signatures(path: Path) -> list[str]: ...
def _fn_sig(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str: ...
def _python_imports(path: Path) -> list[str]: ...
def _generic_signatures(path: Path) -> list[str]: ...
def collect_source_files(repo_root: Path) -> list[Path]: ...
def build_skeleton(repo_root: Path, source_files: list[Path]) -> str: ...
def build_neighborhood(target: Path, repo_root: Path, source_files: list[Path]) -> str: ...
def write_skeleton(repo_root: Path, output_path: Path) -> Path: ...
def _rel_to_module(p: Path) -> str: ...

### garlicpress/swap.py
def _load_summaries(findings_root: Path) -> list[DirectorySummary]: ...
def _load_spec_files(spec_paths: list[Path]) -> list[tuple[str, str]]: ...
def _build_swap_input(summaries: list[DirectorySummary], specs: list[tuple[str, str]]) -> str: ...
async def run_swap(findings_root: Path, spec_files: list[Path], client: LLMClient, config: Config) -> SwapReport | None: ...

### tests/test_schema.py
def _minimal_report() -> dict: ...
def test_minimal_report_roundtrip(): ...
def test_report_with_finding(): ...
def test_implicit_assumption_roundtrip(): ...
def test_invalid_severity_raises(): ...

### tests/test_skeleton.py
def _write(tmp_path: Path, name: str, content: str) -> Path: ...
def test_python_signatures_function(tmp_path): ...
def test_python_signatures_class_and_method(tmp_path): ...
def test_python_imports(tmp_path): ...
def test_collect_source_files(tmp_path): ...
def test_build_skeleton(tmp_path): ...